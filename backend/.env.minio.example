# ========== MinIO 配置 ==========
# MinIO 服务地址（格式：host:port，例如 localhost:9000）
MINIO_ENDPOINT=localhost:9000

# MinIO 访问凭证
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# MinIO 存储桶名称（用于存储提取的图片）
MINIO_BUCKET_NAME=llm-graph-images

# 是否使用 HTTPS（本地开发通常设为 false）
MINIO_USE_SSL=false
